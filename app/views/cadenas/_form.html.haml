= form_for @cadena do |f|
  - if @cadena.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cadena.errors.count, "error")} prohibited this cadena from being saved:"
      %ul
        - @cadena.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :logoname
    = f.text_field :logoname
  .actions
    = f.submit 'Save'
